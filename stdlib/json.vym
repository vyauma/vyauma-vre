// Vyauma JSON Standard Library Module

// Parses a JSON string into Vyauma Objects (Dictionaries, Arrays, Primitives)
fn json_parse(json_str) {
    return ffi_json_parse(json_str);
}

// Stringifies a Vyauma Object into a JSON string
fn json_stringify(obj) {
    return ffi_json_stringify(obj);
}
